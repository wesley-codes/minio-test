apiVersion: v1
kind: Service
metadata:
  name: minio-service
  namespace: minio 
spec:
  ports:
    - name: api
      port: 9000
      targetPort: 9000

    - name: webui
      port: 9001
      targetPort: 9001
  selector:
    app: minio
  type: NodePort
